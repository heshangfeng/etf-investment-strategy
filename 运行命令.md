# ETF 智能投研系统 — 操作命令

## 📊 运行分析

### 午后全量分析（15:00 后）
```powershell
python etf-agent.py
```
全量分析 40 只 ETF，每只 12 个 Agent 并行分析 + 辩论 + 首席决策 + 模拟自动交易。
输出报告到 `output/` 目录，快照到 `data/snapshots/`。

### 晨盘增量分析（8:15 开盘前）
```powershell
python morning_update.py
```
轻量增量更新：加载昨日快照 + 宏观Agent更新 + 规则重评分 + 操盘指导输出。
**不调 LLM，纯规则评分**，适合开盘前快速获取今日操作要点。

```powershell
.\run_morning.ps1
```
晨盘编排脚本：执行 `morning_update.py` 并通过 PushDeer 推送操盘指导到手机。

---

## 💼 实盘持仓管理

```powershell
python portfolio.py status                          # 查看持仓、盈亏、仓位
python portfolio.py buy   <code> <份数> <价格> [佣金]  # 买入记录
python portfolio.py sell  <code> <份数> [价格] [佣金]  # 卖出记录（不填价=按市价）
python portfolio.py cash  <金额>                     # 设置初始现金
```

示例：
```powershell
python portfolio.py cash 100000                      # 初始资金 10 万
python portfolio.py buy 159915 10000 4.50            # 买入创业板ETF 1万份 @ 4.50
python portfolio.py sell 159915 5000                 # 卖出 5000 份（按最新价）
```

---

## 🤖 自动化模拟交易

```powershell
python autotrade.py                  # 查看策略表现（收益率/夏普/回撤）
python autotrade.py trades           # 查看历史交易记录
python autotrade.py perf             # 查看表现详情
```

每次运行 `python etf-agent.py` 分析完成后，系统自动：
1. 读取分析建议（买入/持有/卖出）
2. 对比当前持仓，自动生成买卖指令
3. 执行模拟交易，更新持仓
4. 记录每日资产快照和策略表现

---

## 📈 看板

```powershell
# 方案A：前台运行（终端开着就不停，Ctrl+C 停止）
powershell -File start_dashboard.ps1

# 方案B：后台静默启动（关闭终端也不受影响）
Start-Process -WindowStyle Hidden -FilePath "powershell" -ArgumentList "-File start_dashboard.ps1"
```
启动本地 Web 看板（自动重启），访问 http://localhost:8501
查看：持仓表、Agent 评分明细、投资组合盈亏、历史快照。

---

## ⚙️ 安装依赖

```powershell
python install.py
pip install -r requirements.txt
```

---

## 🤖 自动运行

### 晨盘 8:15 — 操盘指导推送
```powershell
.\run_morning.ps1
```
增量更新 + 操盘指导推送。建议通过 Windows Task Scheduler 自动触发。

**计划任务配置：**
| 项目 | 值 |
|------|-----|
| 名称 | ETF Morning Analysis |
| 触发器 | 每天, 08:15 |
| 程序 | `powershell` |
| 参数 | `-ExecutionPolicy Bypass -File "D:\project\ETF投资策略\run_morning.ps1"` |

### 午后 15:30 — 全量分析 + 复盘推送
```powershell
.\run_daily.ps1
```
全量分析 + 复盘 + PushDeer 推送（含策略表现、模拟盘盈亏、调仓明细）。

**计划任务配置：**
| 项目 | 值 |
|------|-----|
| 名称 | ETF Daily Analysis |
| 触发器 | 每天, 15:30 |
| 程序 | `powershell` |
| 参数 | `-ExecutionPolicy Bypass -File "D:\project\ETF投资策略\run_daily.ps1"` |

### GitHub Actions（自动，仅午后）
每天 15:30 CST（周一至周五）自动运行，无需手动操作。
手动触发：在 GitHub 仓库 Actions 页面点 "Run workflow"，
或命令行：`gh workflow run daily.yml`

---

## 📦 产出文件

| 路径 | 说明 | 保留策略 |
|------|------|---------|
| `output/ETF_*_*.txt` | 完整投研报告（含每 Agent 原文 + 辩论 + 首席决策） | 30 天自动清理 |
| `output/ETF_*_*.xlsx` | 投研摘要看板 | 30 天自动清理 |
| `data/snapshots/{yyyymmdd}.json` | 每日快照（复盘用） | 永久保留 |
| `data/portfolio.json` | 实盘持仓数据 | 用户管理 |
| `data/trade_log.json` | 模拟交易记录 | 自动记录 |
| `data/performance.json` | 策略累计表现（收益率/夏普/回撤） | 自动更新 |
| `data/cache.db` | SQLite 缓存 | 自动管理，WAL 模式 |
| `data/review/cumulative_stats.json` | T+1 复盘累计统计 | 自动更新 |
| `logs/{yyyymmdd}_run.log` | 运行日志 | 手动清理 |

---



## 📁 项目结构

```
ETF投资策略/
├── agents/
│   ├── __init__.py  — Agent统一导出（12个类）
│   ├── base.py  — BaseLLMAgent 基类：LLM调用 + 规则fallback
│   ├── fundamental.py  — 基本面 Agent：价值/技术/情绪/资金/风控/行业（6个）
│   ├── macro.py  — 宏观/货币政策/政策事件 Agent（3个）
│   └── market.py  — 市场 Agent：零售/跨市场/游资/解禁/形态/趋势（6个）
├── core/
│   ├── __init__.py
│   ├── config.py  — 全局配置：ETF标的池、LLM双模型、路径常量
│   ├── data.py  — 数据采集（akshare）+ 规则评分Agent + 回测引擎
│   ├── debate.py  — 辩论引擎：Agent分歧检测 + LLM仲裁
│   ├── decision.py  — 首席决策Agent：综合评分 + compute_rule_score() 静态方法
│   ├── keywords.py  — 行业/情绪关键词库
│   ├── models.py  — 数据类：AgentReport、FinalResearchReport、LLMOutput
│   ├── report.py  — 报告生成：控制台摘要、xlsx看板、txt全文
│   └── scheduler.py  — 主控调度器：三段式多智能体管线
├── data/
│   ├── review/  — 复盘数据：累积统计、Agent权重、优化建议
│   │   ├── cumulative_stats.json
│   │   └── optimization.json
│   ├── snapshots/  — 每日分析快照（供复盘和晨盘使用）
│   │   ├── 20260602.json
│   │   └── 20260603.json
│   ├── _check_consistency.py
│   ├── cache.db  — SQLite 数据缓存
│   ├── performance.json  — 模拟盘策略表现（收益率/夏普/日盈亏）
│   ├── portfolio.json  — 实盘持仓数据
│   └── trade_log.json  — 模拟交易记录 + 每日资产快照
├── docs/
│   ├── superpowers/
│   │   ├── plans/  — 实施计划文档
│   │   │   ├── 2026-06-01-etf-dimension-expansion.md
│   │   │   ├── 2026-06-01-etf-multi-agent-implementation.md
│   │   │   └── 2026-06-04-scheduled-analysis-implementation.md
│   │   └── specs/  — 设计规格文档
│   │       ├── 2026-06-01-etf-multi-agent-research-design.md
│   │       └── 2026-06-04-scheduled-analysis-design.md
│   └── file_descriptions.json  — 本文件：项目文件描述清单
├── infra/
│   ├── __init__.py
│   ├── cache.py  — SQLite持久缓存层（WAL模式、TTL管理）
│   ├── memory.py  — BM25轻量记忆系统：历史分析记录检索
│   └── sentiment_skill.py  — 舆情情绪分析：新闻情感打分
├── scripts/
│   └── generate_structure.py
├── trading/
│   ├── __init__.py
│   ├── autotrade.py  — 自动化模拟交易引擎：信号过滤、调仓、每日快照
│   ├── director.py  — 工作流总监：10维度质量审查
│   ├── portfolio.py  — 实盘持仓管理 + 组合优化（风险平价/均值-方差）
│   └── review.py  — 复盘引擎：T+1验证、Agent准确率追踪、权重更新
├── __init__.py
├── dashboard.py  — Streamlit 投研看板
├── etf-agent.py  — 午后全量分析入口
├── install.py  — 依赖安装脚本
├── morning_update.py  — 晨盘增量分析入口
├── requirements.txt
├── run_daily.ps1  — 午后全量编排推送（15:30 → PushDeer）
├── run_morning.ps1  — 晨盘编排推送（8:15 → PushDeer）
├── start_dashboard.ps1  — 看板启动脚本
├── 系统工作流程与Agent说明.md
├── 运行命令.md  — 操作命令速查
└── 项目Skills使用说明.md  — OpenCode Skills 使用说明
```
