# ETF 智能投研系统 — 操作命令

## 📊 运行分析

### 午后全量分析（15:00 后）
```powershell
python etf-agent.py
```
全量分析 40 只 ETF，每只 12 个 Agent 并行分析 + 辩论 + 首席决策 + 模拟自动交易。
输出报告到 `output/` 目录，快照到 `data/snapshots/`。

### 晨盘增量分析（8:15 开盘前）
```powershell
python morning_update.py
```
轻量增量更新：加载昨日快照 + 宏观Agent更新 + 规则重评分 + 操盘指导输出。
**不调 LLM，纯规则评分**，适合开盘前快速获取今日操作要点。

```powershell
.\run_morning.ps1
```
晨盘编排脚本：执行 `morning_update.py` 并通过 PushDeer 推送操盘指导到手机。

---

## 💼 实盘持仓管理

```powershell
python portfolio.py status                          # 查看持仓、盈亏、仓位
python portfolio.py buy   <code> <份数> <价格> [佣金]  # 买入记录
python portfolio.py sell  <code> <份数> [价格] [佣金]  # 卖出记录（不填价=按市价）
python portfolio.py cash  <金额>                     # 设置初始现金
```

示例：
```powershell
python portfolio.py cash 100000                      # 初始资金 10 万
python portfolio.py buy 159915 10000 4.50            # 买入创业板ETF 1万份 @ 4.50
python portfolio.py sell 159915 5000                 # 卖出 5000 份（按最新价）
```

---

## 🤖 自动化模拟交易

```powershell
python autotrade.py                  # 查看策略表现（收益率/夏普/回撤）
python autotrade.py trades           # 查看历史交易记录
python autotrade.py perf             # 查看表现详情
```

每次运行 `python etf-agent.py` 分析完成后，系统自动：
1. 读取分析建议（买入/持有/卖出）
2. 对比当前持仓，自动生成买卖指令
3. 执行模拟交易，更新持仓
4. 记录每日资产快照和策略表现

---

## 📈 看板

```powershell
# 方案A：前台运行（终端开着就不停，Ctrl+C 停止）
powershell -File start_dashboard.ps1

# 方案B：后台静默启动（关闭终端也不受影响）
Start-Process -WindowStyle Hidden -FilePath "powershell" -ArgumentList "-File start_dashboard.ps1"
```
启动本地 Web 看板（自动重启），访问 http://localhost:8501
查看：持仓表、Agent 评分明细、投资组合盈亏、历史快照。

---

## ⚙️ 安装依赖

```powershell
python install.py
pip install -r requirements.txt
```

---

## 🤖 自动运行

### 晨盘 8:15 — 操盘指导推送
```powershell
.\run_morning.ps1
```
增量更新 + 操盘指导推送。建议通过 Windows Task Scheduler 自动触发。

**计划任务配置：**
| 项目 | 值 |
|------|-----|
| 名称 | ETF Morning Analysis |
| 触发器 | 每天, 08:15 |
| 程序 | `powershell` |
| 参数 | `-ExecutionPolicy Bypass -File "D:\project\ETF投资策略\run_morning.ps1"` |

### 午后 15:30 — 全量分析 + 复盘推送
```powershell
.\run_daily.ps1
```
全量分析 + 复盘 + PushDeer 推送（含策略表现、模拟盘盈亏、调仓明细）。

**计划任务配置：**
| 项目 | 值 |
|------|-----|
| 名称 | ETF Daily Analysis |
| 触发器 | 每天, 15:30 |
| 程序 | `powershell` |
| 参数 | `-ExecutionPolicy Bypass -File "D:\project\ETF投资策略\run_daily.ps1"` |

### GitHub Actions（自动，仅午后）
每天 15:30 CST（周一至周五）自动运行，无需手动操作。
手动触发：在 GitHub 仓库 Actions 页面点 "Run workflow"，
或命令行：`gh workflow run daily.yml`

---

## 📦 产出文件

| 路径 | 说明 | 保留策略 |
|------|------|---------|
| `output/ETF_*_*.txt` | 完整投研报告（含每 Agent 原文 + 辩论 + 首席决策） | 30 天自动清理 |
| `output/ETF_*_*.xlsx` | 投研摘要看板 | 30 天自动清理 |
| `data/snapshots/{yyyymmdd}.json` | 每日快照（复盘用） | 永久保留 |
| `data/portfolio.json` | 实盘持仓数据 | 用户管理 |
| `data/trade_log.json` | 模拟交易记录 | 自动记录 |
| `data/performance.json` | 策略累计表现（收益率/夏普/回撤） | 自动更新 |
| `data/cache.db` | SQLite 缓存 | 自动管理，WAL 模式 |
| `data/review/cumulative_stats.json` | T+1 复盘累计统计 | 自动更新 |
| `logs/{yyyymmdd}_run.log` | 运行日志 | 手动清理 |

---

## 📁 项目结构

```
ETF投资策略/
├── etf-agent.py             午后全量入口
├── morning_update.py        晨盘增量入口
├── dashboard.py             Streamlit 看板
├── install.py               依赖安装
│
├── core/                    核心分析管线
│   ├── config.py            全局配置（ETF 池、LLM 双模型、路径）
│   ├── models.py            数据类（AgentReport/FinalResearchReport/LLMOutput）
│   ├── data.py              数据采集 + 规则评分 + 回测
│   ├── keywords.py          关键词库
│   ├── debate.py            辩论引擎
│   ├── decision.py          首席决策 + 规则评分静态方法
│   ├── report.py            报告生成（xlsx + txt）
│   └── scheduler.py         主控调度器
│
├── trading/                 交易与复盘
│   ├── autotrade.py         自动化模拟交易引擎
│   ├── portfolio.py         实盘持仓管理 + 组合优化
│   ├── review.py            复盘引擎（T+1 验证）
│   └── director.py          工作流总监审查
│
├── infra/                   基础设施
│   ├── cache.py             SQLite 持久缓存（WAL 模式）
│   ├── memory.py            BM25 轻量记忆系统
│   └── sentiment_skill.py   舆情情绪分析
│
├── agents/                  多智能体（12 个 Agent）
│   ├── base.py              BaseLLMAgent 基类
│   ├── macro.py             宏观 / 货币 / 政策 Agent
│   ├── fundamental.py       价值 / 技术 / 情绪 / 资金 / 风控 / 行业 Agent
│   └── market.py            零售 / 跨市场 / 游资 / 解禁 / 形态 Agent
│
├── run_morning.ps1          晨盘编排推送（8:15）
├── run_daily.ps1            午后全量编排推送（15:30）
├── requirements.txt         依赖清单
├── .env                     API Key（已 gitignore）
│
├── output/                  报告输出目录
└── data/                    数据目录
    ├── snapshots/           每日快照
    ├── portfolio.json       实盘持仓
    ├── trade_log.json       模拟交易记录
    ├── performance.json     策略表现数据
    ├── cache.db             SQLite 缓存
    └── review/              复盘数据
```
